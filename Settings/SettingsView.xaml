<UserControl x:Class="PlayniteApiServer.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="520">
    <StackPanel Margin="20">
        <TextBlock Text="Playnite API Server"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,4" />
        <TextBlock TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,16"
                   Text="Exposes a bearer-authenticated HTTP REST API on 127.0.0.1 for reading and writing the Playnite game database." />

        <TextBlock Text="Port" Margin="0,0,0,4" />
        <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}"
                 Width="120" HorizontalAlignment="Left" Margin="0,0,0,12" />

        <TextBlock Text="Bearer Token" Margin="0,0,0,4" />
        <DockPanel LastChildFill="True" Margin="0,0,0,12">
            <Button Content="Regenerate"
                    Command="{Binding RegenerateTokenCommand}"
                    DockPanel.Dock="Right"
                    Margin="8,0,0,0"
                    Padding="12,4" />
            <TextBox Text="{Binding Token, UpdateSourceTrigger=PropertyChanged}"
                     FontFamily="Consolas" />
        </DockPanel>

        <CheckBox Content="Enable write operations (POST / PATCH / DELETE)"
                  IsChecked="{Binding EnableWrites}"
                  Margin="0,4,0,12" />

        <TextBlock TextWrapping="Wrap" Opacity="0.7"
                   Text="The listener binds to http://127.0.0.1:{port}/ only. Changing the token or write flag takes effect immediately. Changing the port restarts the listener in place — no Playnite restart required." />
    </StackPanel>
</UserControl>
